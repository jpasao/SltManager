const defaultCollection = {
  IdCollection: 0,
  CollectionName: '',
  Patreon: {
    IdPatreon: 0,
  },
}
const defaultDelete = { id: 0, name: '', page: 'Colecciones' }

export { defaultCollection, defaultDelete }
